version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - NODE_ENV=production
    restart: unless-stopped

  # Commented out - using Prisma's online database
  # db:
  #   image: postgres:16-alpine
  #   environment:
  #     - POSTGRES_USER=tdog
  #     - POSTGRES_PASSWORD=tdog_password
  #     - POSTGRES_DB=tdog
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  #   restart: unless-stopped

# volumes:
#   postgres_data:
